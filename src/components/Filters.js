const Filters = (props) => {
  return (
    <div>
      <div>
        <label>Filter Price:</label>
        <select>
          <option>filters</option>
        </select>
      </div>
      <div>
        <label>Filter title:</label>
        <select>
          <option>filters</option>
        </select>
      </div>
    </div>
  );
};

export default Filters;
